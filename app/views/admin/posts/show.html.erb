<div class="flex flex-col w-full items-center justify-start">
  <p style="color: green"><%= notice %></p>

  <%= render @admin_post %>

  <div class="mt-4 flex gap-4">
    <%= link_to "Back to posts", admin_posts_path %>
    <%= button_to "Destroy this post", [:admin, @admin_post], method: :delete, data: {'turbo-confirm': "Are you sure?" } %>
  </div>
</div>
